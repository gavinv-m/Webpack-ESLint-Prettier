const name = 'James';

const person = { first: name };

console.log(person);

const sayHelloLinting = (fName) => {
  console.log(`Hello linting, ${fName}`);
};

const otherName = 'James';

const str = `His name is ${otherName}`;
console.log(str);
